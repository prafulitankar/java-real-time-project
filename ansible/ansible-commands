#ansible all -m ping : To run ping command for all the hosts from inventory file.
#ansible webservers -m ping : To run the ping command on webservers group from inventory file.
#ansible dbservers -m ping : To run the ping command on dbservers group from inventory file.
#ansible webservers -m ping
#ansible dbservers -m ping
#ansible all -m ping
#ansible-playbook --syntax-check multiserver.yaml : To check playbook syntax
#ansible-playbook -C multiserver.yaml : To run dry run
#ansible-playbook update.yaml : To run Playbook
#ansible-playbook  -l webservers multiserver.yaml : To run playbook on specific group of server

